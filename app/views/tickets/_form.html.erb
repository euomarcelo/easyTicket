<%= form_for @ticket, :html => { :class => "form-horizontal ticket" } do |f| %>

  <% if @ticket.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@ticket.errors.count, "error") %> prohibited this ticket from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @ticket.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :user_id, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :user_id, :class => 'form-control' %>
    </div>
    <%=f.error_span(:user_id) %>
  </div>
  <div class="form-group">
    <%= f.label :event_name, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :event_name, :class => 'form-control' %>
    </div>
    <%=f.error_span(:event_name) %>
  </div>
  <div class="form-group">
    <%= f.label :event_date, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :event_date, :class => 'form-control' %>
    </div>
    <%=f.error_span(:event_date) %>
  </div>
  <div class="form-group">
    <%= f.label :event_description, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :event_description, :class => 'form-control' %>
    </div>
    <%=f.error_span(:event_description) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                tickets_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
